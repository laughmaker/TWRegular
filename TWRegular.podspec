Pod::Spec.new do |s|

  s.name         = "TWRegular"
  s.version      = "1.0.1"
  s.summary      = "常用正则模块"
  s.homepage     = "https://github.com/laughmaker/TWRegular"
  s.license      = { :type => "MIT", :file => "LICENSE" }
  s.author       = { "hezhendong" => "wskrdfeai@gmail.com" }
  s.platform     = :ios, "7.0"
  s.source       = { :git => "https://github.com/laughmaker/TWRegular.git", :tag => "1.0.1" }
  s.source_files = "TWRegular/*"

end
